export type TranslationWBListQuery = {
    startDate?: Date,
    endDate?: Date,
    action?: string,
    user_id?: string,
    success?: boolean,
    sortBy?: string,
    sortOrder?: string 
}